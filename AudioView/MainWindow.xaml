<Controls:MetroWindow 
        xmlns:UserControls="clr-namespace:AudioView.UserControls"   
        xmlns:AudioView="clr-namespace:AudioView"  x:Class="AudioView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="AudioView" MinHeight="600" MinWidth="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>
            <Controls:Flyout Header="New Measurement" Position="Right" Width="450" IsOpen="{Binding ShowNewFlow}">
                <UserControls:NewMeasurementControl DataContext="{Binding NewViewModel}" />
            </Controls:Flyout>
        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>

    <Grid>
        <TabControl TabStripPlacement="Left"
                Margin="10">
            <TabItem Header="Measurements">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <!--<RowDefinition Height="Auto" />-->
                    </Grid.RowDefinitions>

                    <Menu Grid.Row="0"
                      Margin="5"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Left">
                        <MenuItem Header="Test" Click="MenuItem_OnClick" />
                        <MenuItem Header="New measurement" Command="{Binding StartMeasurement}" />
                        <MenuItem Header="Pop-Outs" IsEnabled="{Binding MeasurementSelected}">
                            <MenuItem Header="Live Readings" Command="{Binding NewLiveReadingsPopUp}" />
                        </MenuItem>
                        <MenuItem Header="Close measurement" IsEnabled="{Binding MeasurementSelected}" Command="{Binding CloseMeasurementCommand}"/>
                    </Menu>

                    <TabControl Grid.Row="1" 
                ItemsSource="{Binding Measurements}"
                SelectedItem="{Binding SelectedMeasurementy}"
                        >
                        <TabControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title}" />
                            </DataTemplate>
                        </TabControl.ItemTemplate>

                        <TabControl.ContentTemplate>
                            <DataTemplate>
                                <UserControls:MeasurementControl DataContext="{Binding}" />
                            </DataTemplate>
                        </TabControl.ContentTemplate>
                    </TabControl>

                    <!--<ProgressBar Grid.Row="2" HorizontalAlignment="Stretch" Minimum="0" Maximum="100" Value="{Binding LagTest}"/>-->
                </Grid>
            </TabItem>
            <TabItem Header="History">
            </TabItem>
            <TabItem Header="Settings">
            </TabItem>
        </TabControl>

    </Grid>
</Controls:MetroWindow>
